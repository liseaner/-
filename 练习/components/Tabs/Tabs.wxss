.tabs{}
.tabs_title{
  display: flex;
}
.title_item{
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 1;
  padding: 15rpx;
}
.active{
  color: var(--themeColor);
  border-bottom:5rpx solid currentColor;
}